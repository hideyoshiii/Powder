<%= render "partial/navbar" %>
<mains class="" id="main-collapse">

<% if @course.went.blank? %>
	<h4 class="text-center">レビューする</h4>
<% else %>
	<h4 class="text-center">レビューを編集する</h4>
 <% end %>

<% @points.each do |point| %>
	<div class="sss-box">
		<div class="ss-img">
			<%= image_tag point.spot.pictures.first.image.url, :class =>'img-sss' %>
		</div>
		<div class="ss-title">
			<%= point.spot.title %>
			<br>
			<span style="font-size:12px;color:#808080;"><%= point.spot.station %></span>
		</div>	
	</div>
	<% end %> 

<%= form_for @course, :html => { multipart: true } do |f| %>

<% if @course.release == false %>
	<%= f.hidden_field :release, :value => true %>
<% end %>

<br>
<div class="text-center">
	<label>このコースを公開しますか？</label>
	<br>
	<span class="radio-city">	
	 	<%= f.radio_button(:went, "公開する", {:checked => true, :id => "yes"}) %>
		<%= label_tag('yes', "公開する") %>
	</span>
	<span class="radio-city">	
	 	<%= f.radio_button(:went, "公開しない", {:id => "no"}) %>
		<%= label_tag('no', "公開しない") %>
	</span>
</div>
<br>

<div class="panel-body">
	<div class="text-center">
		<label>タイトルが公開されます</label>
		<%= f.text_field :title, placeholder: "コースを一言で表すなら？", class: "form-control", required: "true" %>
	</div>
	<br><br>
</div>

	<div class="text-center">
        <label>どのエリアでのデートでしたか？</label>
        <div class="horizontal_scroll_timeline">
	  <ul class="scroll_lst_timeline">
	  	<li class="scroll_item_timeline">
	        <span class="radio-timeline">	
			 	<%= f.radio_button(:city, "恵比寿・代官山・中目黒") %>
				<%= f.label(:city, "恵比寿・代官山・中目黒", value: "恵比寿・代官山・中目黒") %>
			</span>
	    </li>
	  	<li class="scroll_item_timeline">
	        <span class="radio-timeline">	
			 	<%= f.radio_button(:city, "渋谷") %>
				<%= f.label(:city, "渋谷", value: "渋谷") %>
			</span>
	    </li>
	    <li class="scroll_item_timeline">
	        <span class="radio-timeline">	
			 	<%= f.radio_button(:city, "原宿・表参道・青山") %>
				<%= f.label(:city, "原宿・表参道・青山", value: "原宿・表参道・青山") %>
			</span>
	    </li>
	    <li class="scroll_item_timeline">
	        <span class="radio-timeline">	
			 	<%= f.radio_button(:city, "新宿") %>
				<%= f.label(:city, "新宿", value: "新宿") %>
			</span>
	    </li>
	  </ul>
	</div>
    </div>
    <br><br>

	<div class="text-center">
        <label>実際にどれくらのお金を使いましたか？</label>
        <div class="horizontal_scroll_timeline">
	  <ul class="scroll_lst_timeline">
	  	<li class="scroll_item_timeline">
	        <span class="radio-timeline">	
			 	<%= f.radio_button(:price_used, 2000) %>
				<%= f.label(:price_used_2000, "~2,000円/人") %>
			</span>
	    </li>
	  	<li class="scroll_item_timeline">
	        <span class="radio-timeline">	
			 	<%= f.radio_button(:price_used, 4000) %>
				<%= f.label(:price_used_4000, "~4,000円/人") %>
			</span>
	    </li>
	    <li class="scroll_item_timeline">
	        <span class="radio-timeline">	
			 	<%= f.radio_button(:price_used, 6000) %>
				<%= f.label(:price_used_6000, "~6,000円/人") %>
			</span>
	    </li>
	    <li class="scroll_item_timeline">
	        <span class="radio-timeline">	
			 	<%= f.radio_button(:price_used, 8000) %>
				<%= f.label(:price_used_8000, "~8,000円/人") %>
			</span>
	    </li>
	    <li class="scroll_item_timeline">
	        <span class="radio-timeline">	
			 	<%= f.radio_button(:price_used, 10000) %>
				<%= f.label(:price_used_10000, "~10,000円/人") %>
			</span>
	    </li>
	    <li class="scroll_item_timeline">
	        <span class="radio-timeline">	
			 	<%= f.radio_button(:price_used, 12000) %>
				<%= f.label(:price_used_12000, "~12,000円/人") %>
			</span>
	    </li>
	    <li class="scroll_item_timeline">
	        <span class="radio-timeline">	
			 	<%= f.radio_button(:price_used, 14000) %>
				<%= f.label(:price_used_14000, "~14,000円/人") %>
			</span>
	    </li>
	    <li class="scroll_item_timeline">
	        <span class="radio-timeline">	
			 	<%= f.radio_button(:price_used, 16000) %>
				<%= f.label(:price_used_16000, "~16,000円/人") %>
			</span>
	    </li>
	    <li class="scroll_item_timeline">
	        <span class="radio-timeline">	
			 	<%= f.radio_button(:price_used, 18000) %>
				<%= f.label(:price_used_18000, "~18,000円/人") %>
			</span>
	    </li>
	    <li class="scroll_item_timeline">
	        <span class="radio-timeline">	
			 	<%= f.radio_button(:price_used, 20000) %>
				<%= f.label(:price_used_20000, "~20,000円/人") %>
			</span>
	    </li>
	    <li class="scroll_item_timeline">
	        <span class="radio-timeline">	
			 	<%= f.radio_button(:price_used, 22000) %>
				<%= f.label(:price_used_22000, "~22,000円/人") %>
			</span>
	    </li>
	    <li class="scroll_item_timeline">
	        <span class="radio-timeline">	
			 	<%= f.radio_button(:price_used, 14000) %>
				<%= f.label(:price_used_24000, "~24,000円/人") %>
			</span>
	    </li>
	    <li class="scroll_item_timeline">
	        <span class="radio-timeline">	
			 	<%= f.radio_button(:price_used, 26000) %>
				<%= f.label(:price_used_26000, "~26,000円/人") %>
			</span>
	    </li>
	    <li class="scroll_item_timeline">
	        <span class="radio-timeline">	
			 	<%= f.radio_button(:price_used, 28000) %>
				<%= f.label(:price_used_28000, "~28,000円/人") %>
			</span>
	    </li>
	    <li class="scroll_item_timeline">
	        <span class="radio-timeline">	
			 	<%= f.radio_button(:price_used, 30000) %>
				<%= f.label(:price_used_30000, "30,000円/人~") %>
			</span>
	    </li>
	  </ul>
	</div>
    </div>
    <br><br>

<div class="panel-body">
	<div class="text-center">
		<label>写真を４枚まで追加できます</label>
	</div>
</div>

<div class="horizontal_scroll_wrap_pic">
<ul class="scroll_lst_pic">
  	<%# 横スクロールコンテンツ %>
  	<% if @picture1 = Picture.where(course_id: @course.id).order(id: "ASC").first %>
	  	<li class="scroll_item_pic">
		    <div class="scroll_item_inr_pic">
			    <%= image_tag @picture1.image.url, :class =>'img-pic' %>
		      	<div class="scroll_item_info_pic_quote">
		      		<div class="text-center">
			    		<p class="scroll_item_ttl_pic_quote">
			    			<%= @picture1.spot.title %>
						</p>
						<div class="btn btn-default btn-sm" style="margin:10px 0px;">
							<%= link_to("/pictures/#{@picture1.id}/destroy3", {method: "post"}) do %>
								<span style="color:red;">削除</span>
							<% end %>
						</div>	
					</div>
		      	</div>
		   </div>
		</li>
  	<% else %>
	  	<li class="scroll_item_pic">
		    <div class="scroll_item_inr_pic">
			    <span class="form1">
				<label>
			    <%= f.file_field :images, multiple: true %>
			    <span class="preview"></span>
			    </label>
				</span>
		      <div class="scroll_item_info_pic_quote">
		    	<% @points.each do |point| %>
		    	<% @n1 = @n1 + 1 %>
		    		<p class="scroll_item_ttl_pic_quote">
						<% @spot = Spot.find(point.spot_id) %>
						<% if @n1 == 1 %>
		        			<label>
		          				<%= radio_button_tag :spot1, @spot.id, true %>&nbsp;<%= @spot.title %>
							<label>	
						<% else %>  
							<label>
		          				<%= radio_button_tag :spot1, @spot.id, false %>&nbsp;<%= @spot.title %>
							<label>	
						<% end %>  		
					</p>	
				<% end %>  
		      </div>
		   	</div>
	  	</li>
  	<% end %>
  	<% if @picture2 = Picture.where(course_id: @course.id).order(id: "ASC").second %>
	  	<li class="scroll_item_pic">
		    <div class="scroll_item_inr_pic">
			    <%= image_tag @picture2.image.url, :class =>'img-pic' %>
		      	<div class="scroll_item_info_pic_quote">
		      		<div class="text-center">
			    		<p class="scroll_item_ttl_pic_quote">
			    			<%= @picture2.spot.title %>
						</p>
						<div class="btn btn-default btn-sm" style="margin:10px 0px;">
							<%= link_to("/pictures/#{@picture2.id}/destroy3", {method: "post"}) do %>
								<span style="color:red;">削除</span>
							<% end %>
						</div>	
					</div>
		      	</div>
		   </div>
		</li>
  	<% else %>
	  	<li class="scroll_item_pic">
		    <div class="scroll_item_inr_pic">
		        <span class="form2">
				<label>
			    <%= f.file_field :images, multiple: true %>
			    <span class="preview2"></span>
			    </label>
				</span>
		      <div class="scroll_item_info_pic_quote">
		        <% @points.each do |point| %>
		    	<% @n2 = @n2 + 1 %>
		    		<p class="scroll_item_ttl_pic_quote">
						<% @spot = Spot.find(point.spot_id) %>
						<% if @n2 == 1 %>
		        			<label>
		          				<%= radio_button_tag :spot2, @spot.id, true %>&nbsp;<%= @spot.title %>
							<label>	
						<% else %>  
							<label>
		          				<%= radio_button_tag :spot2, @spot.id, false %>&nbsp;<%= @spot.title %>
							<label>	
						<% end %>  		
					</p>	
				<% end %>  
		      </div>
		   	</div>
	  	</li>
	<% end %>
	<% if @picture3 = Picture.where(course_id: @course.id).order(id: "ASC").third %>
	  	<li class="scroll_item_pic">
		    <div class="scroll_item_inr_pic">
			    <%= image_tag @picture3.image.url, :class =>'img-pic' %>
		      	<div class="scroll_item_info_pic_quote">
		      		<div class="text-center">
			    		<p class="scroll_item_ttl_pic_quote">
			    			<%= @picture3.spot.title %>
						</p>
						<div class="btn btn-default btn-sm" style="margin:10px 0px;">
							<%= link_to("/pictures/#{@picture3.id}/destroy3", {method: "post"}) do %>
								<span style="color:red;">削除</span>
							<% end %>
						</div>	
					</div>
		      	</div>
		   </div>
		</li>
  	<% else %>
	  	<li class="scroll_item_pic">
		    <div class="scroll_item_inr_pic">
			    <span class="form3">
				<label>
			    <%= f.file_field :images, multiple: true %>
			    <span class="preview3"></span>
			    </label>
				</span>
		      <div class="scroll_item_info_pic_quote">
		    	<% @points.each do |point| %>
		    	<% @n3 = @n3 + 1 %>
		    		<p class="scroll_item_ttl_pic_quote">
						<% @spot = Spot.find(point.spot_id) %>
						<% if @n3 == 1 %>
		        			<label>
		          				<%= radio_button_tag :spot3, @spot.id, true %>&nbsp;<%= @spot.title %>
							<label>	
						<% else %>  
							<label>
		          				<%= radio_button_tag :spot3, @spot.id, false %>&nbsp;<%= @spot.title %>
							<label>	
						<% end %>  		
					</p>	
				<% end %>  
		      </div>
		   	</div>
	  	</li>
  	<% end %>
  	<% if @picture4 = Picture.where(course_id: @course.id).order(id: "ASC").fourth %>
	  	<li class="scroll_item_pic">
		    <div class="scroll_item_inr_pic">
			    <%= image_tag @picture4.image.url, :class =>'img-pic' %>
		      	<div class="scroll_item_info_pic_quote">
		      		<div class="text-center">
			    		<p class="scroll_item_ttl_pic_quote">
			    			<%= @picture4.spot.title %>
						</p>
						<div class="btn btn-default btn-sm" style="margin:10px 0px;">
							<%= link_to("/pictures/#{@picture4.id}/destroy3", {method: "post"}) do %>
								<span style="color:red;">削除</span>
							<% end %>
						</div>	
					</div>
		      	</div>
		   </div>
		</li>
  	<% else %>
	  	<li class="scroll_item_pic">
		    <div class="scroll_item_inr_pic">
			    <span class="form4">
				<label>
			    <%= f.file_field :images, multiple: true %>
			    <span class="preview4"></span>
			    </label>
				</span>
		      <div class="scroll_item_info_pic_quote">
		    	<% @points.each do |point| %>
		    	<% @n4 = @n4 + 1 %>
		    		<p class="scroll_item_ttl_pic_quote">
						<% @spot = Spot.find(point.spot_id) %>
						<% if @n4 == 1 %>
		        			<label>
		          				<%= radio_button_tag :spot4, @spot.id, true %>&nbsp;<%= @spot.title %>
							<label>	
						<% else %>  
							<label>
		          				<%= radio_button_tag :spot4, @spot.id, false %>&nbsp;<%= @spot.title %>
							<label>	
						<% end %>  		
					</p>	
				<% end %>  
		      </div>
		   	</div>
	  	</li>
  	<% end %>
</ul>
</div>
<br><br>

<div class="panel-body">

	<div class="text-center">
    <label>このコースについて</label>
    	<%= f.text_area :good_point, rows: 8, placeholder: "実際にこのコースでデートしてみてどうでしたか？", class: "form-control" %>
	</div>
	<br><br><br>

	<div class="text-center">
		<%= f.submit "保存", class: "btn btn-primary" %>
	</div>	
	<br>

</div>
	
<% end %>

<script>
$(function(){
  //画像ファイルプレビュー表示のイベント追加 fileを選択時に発火するイベントを登録
  $('.form1').on('change', 'input[type="file"]', function(e) {
    var file = e.target.files[0],
        reader = new FileReader(),
        $preview = $(".preview");
        t = this;

    // 画像ファイル以外の場合は何もしない
    if(file.type.indexOf("image") < 0){
      return false;
    }

    // ファイル読み込みが完了した際のイベント登録
    reader.onload = (function(file) {
      return function(e) {
        //既存のプレビューを削除
        $preview.empty();
        // .prevewの領域の中にロードした画像を表示するimageタグを追加
        $preview.append($('<img>').attr({
                  src: e.target.result,
                  class: "preview",
                  title: file.name
              }));
      };
    })(file);

    reader.readAsDataURL(file);
  });
});
</script>

<script>
$(function(){
  //画像ファイルプレビュー表示のイベント追加 fileを選択時に発火するイベントを登録
  $('.form2').on('change', 'input[type="file"]', function(e) {
    var file = e.target.files[0],
        reader = new FileReader(),
        $preview = $(".preview2");
        t = this;



    // 画像ファイル以外の場合は何もしない
    if(file.type.indexOf("image") < 0){
      return false;
    }

    // ファイル読み込みが完了した際のイベント登録
    reader.onload = (function(file) {
      return function(e) {
        //既存のプレビューを削除
        $preview.empty();
        // .prevewの領域の中にロードした画像を表示するimageタグを追加
        $preview.append($('<img>').attr({
                  src: e.target.result,
                  class: "preview",
                  title: file.name
              }));
      };
    })(file);

    reader.readAsDataURL(file);
  });
});
</script>

<script>
$(function(){
  //画像ファイルプレビュー表示のイベント追加 fileを選択時に発火するイベントを登録
  $('.form3').on('change', 'input[type="file"]', function(e) {
    var file = e.target.files[0],
        reader = new FileReader(),
        $preview = $(".preview3");
        t = this;

    // 画像ファイル以外の場合は何もしない
    if(file.type.indexOf("image") < 0){
      return false;
    }

    // ファイル読み込みが完了した際のイベント登録
    reader.onload = (function(file) {
      return function(e) {
        //既存のプレビューを削除
        $preview.empty();
        // .prevewの領域の中にロードした画像を表示するimageタグを追加
        $preview.append($('<img>').attr({
                  src: e.target.result,
                  class: "preview",
                  title: file.name
              }));
      };
    })(file);

    reader.readAsDataURL(file);
  });
});
</script>

<script>
$(function(){
  //画像ファイルプレビュー表示のイベント追加 fileを選択時に発火するイベントを登録
  $('.form4').on('change', 'input[type="file"]', function(e) {
    var file = e.target.files[0],
        reader = new FileReader(),
        $preview = $(".preview4");
        t = this;

    // 画像ファイル以外の場合は何もしない
    if(file.type.indexOf("image") < 0){
      return false;
    }

    // ファイル読み込みが完了した際のイベント登録
    reader.onload = (function(file) {
      return function(e) {
        //既存のプレビューを削除
        $preview.empty();
        // .prevewの領域の中にロードした画像を表示するimageタグを追加
        $preview.append($('<img>').attr({
                  src: e.target.result,
                  class: "preview",
                  title: file.name
              }));
      };
    })(file);

    reader.readAsDataURL(file);
  });
});
</script>


</mains>

<script>
document.addEventListener("DOMContentLoaded", function (event) {
  navbarToggleSidebar();
  navActivePage();
});
</script>

<!-- Google Analytics: change UA-XXXXX-X to be your site's ID 

<script>
  (function (i, s, o, g, r, a, m) {
    i['GoogleAnalyticsObject'] = r; i[r] = i[r] || function () {
      (i[r].q = i[r].q || []).push(arguments)
    }, i[r].l = 1 * new Date(); a = s.createElement(o),
      m = s.getElementsByTagName(o)[0]; a.async = 1; a.src = g; m.parentNode.insertBefore(a, m)
  })(window, document, 'script', '//www.google-analytics.com/analytics.js', 'ga');
  ga('create', 'UA-XXXXX-X', 'auto');
  ga('send', 'pageview');
</script>

--> <script type="text/javascript" src="./main.85741bff.js"></script></body>
